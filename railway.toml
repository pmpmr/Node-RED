[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node-red --userDir /data --settings /app/settings.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10